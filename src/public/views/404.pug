doctype html

html(lang='en')
  include includes/head.pug
  body
    svg(viewBox="0 0 600 300")
      symbol#s-text
        text(text-anchor="middle", x="50%", y="50%", dy=".35em")
          | 404 ERROR
      //- Duplicate symbols
      use.text(xlink:href="#s-text")
      use.text(xlink:href="#s-text")
      use.text(xlink:href="#s-text")
      use.text(xlink:href="#s-text")
      use.text(xlink:href="#s-text")
    .safety
      button#safety: a(href='/collection') Back to Safety